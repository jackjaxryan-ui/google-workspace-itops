# Scripts

Files:
- csv_to_group_actions.py — Converts user roles from CSV into group membership actions
- sample_users.csv — Sample input data

## csv_to_group_actions.py
Reads `sample_users.csv` and prints proposed Google Group membership actions.

### Run
```bash
python3 csv_to_group_actions.py
